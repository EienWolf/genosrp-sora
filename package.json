{
  "name": "genosrp-sora",
  "private": true,
  "version": "0.1.0",
  "description": "Sitio estático GenosRP Sora, publicado en Cloudflare Workers.",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8000",
    "build": "node scripts/generar.mjs",
    "preview": "npm run build && wrangler dev",
    "deploy": "./scripts/deploy.sh",
    "setup:dns": "./scripts/setup-dns.sh"
  },
  "devDependencies": {
    "js-yaml": "^5.4.1",
    "lenis": "^1.3.26",
    "marked": "^18.0.11",
    "wrangler": "^4.129.0"
  }
}
